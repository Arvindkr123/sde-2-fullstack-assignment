# Backend
PORT=4000
# Required — generate with: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=
JWT_EXPIRES_IN=7d

# MySQL
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=sequencer

# Redis
REDIS_HOST=127.0.0.1
REDIS_PORT=6379

# Seed script only — password hashed with bcrypt before storage, never stored in plain text
SEED_PASSWORD=

# Frontend (Vite reads VITE_*)
VITE_API_URL=http://localhost:4000
